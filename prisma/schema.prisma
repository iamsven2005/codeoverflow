generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model user {
  clerkId       String            @default(cuid()) @id
  first_name    String?
  last_name     String?
  image_url     String?
  email         String? @unique
}
